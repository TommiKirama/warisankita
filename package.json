{
  "name": "warisankita",
  "version": "1.0.0",
  "private": true,
  "description": "WarisanKita — AI-Powered Islamic Estate Planning Platform (Smart Wasiyyah & Faraid Distribution)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "node node_modules/concurrently/dist/bin/concurrently.js -n server,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
